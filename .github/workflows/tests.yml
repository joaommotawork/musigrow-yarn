name: Run All Tests
on:
  push:
    branches:
     - '**'
  pull_request:
    branches:
     - '**'
jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: "14.x"
      - name: Install dependencies
        run: yarn install
      - name: Change Directory to Musigrow App
        run: cd apps/musigrow-app
      - name: Install Playwright Musigrow App
        run: npx playwright install --with-deps
      - name: Run Jest Musigrow App
        run: yarn test
      - name: Run Playwright Tests Musigrow App
        run: yarn test:e2e
      - name: Change Directory to Musigrow UI
        run: cd ../../packages/musigrow-ui
      - name: Install Playwright Musigrow UI
        run: npx playwright install --with-deps
      - name: Run Jest Musigrow UI
        run: yarn test
      - name: Run Playwright Tests Musigrow UI
        run: yarn test:e2e
